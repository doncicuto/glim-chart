apiVersion: v1
kind: Service
metadata:
  name: {{ include "glim.fullname" . }}
  labels:
    {{- include "glim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glim.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glim.fullname" . }}-test-connection"
  labels:
    {{- include "glim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glim.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
